spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/task_master?useSSL=false
    username:  ${MYSQL_USER}
    password:  ${MYSQL_PASSWORD}
